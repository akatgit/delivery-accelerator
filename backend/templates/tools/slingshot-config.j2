{# version: 1.0.0 #}
{# tool: slingshot #}
{# description: Structural skeleton for a Slingshot AI assistant configuration, filled in by generate-tool-config #}
# Slingshot configuration for {{ project_name }}
project:
  name: "{{ project_name }}"
  description: "{{ project_description }}"

# generate-tool-config fills in each rules block below with project-specific
# guidance derived from instructions.md. Only accepted findings and chosen
# contradiction resolutions may inform this content (FR-4.2).
rules:
  architecture: |
    [ARCHITECTURE GUIDANCE]
  coding_standards: |
    [CODING GUIDANCE]
  api_design: |
    [API DESIGN GUIDANCE]
  security: |
    [SECURITY GUIDANCE]
  testing: |
    [TESTING GUIDANCE]
  naming_conventions: |
    [NAMING GUIDANCE]
